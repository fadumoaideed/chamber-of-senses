/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: {
    emotion: true, // Enables Emotion support natively in Next.js
  },
};

export default nextConfig;
